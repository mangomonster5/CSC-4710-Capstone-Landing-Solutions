{
  "name": "project_files2",
  "version": "0.1.0",
  "private": true,

  "scripts": {
    "setup": "npm install --prefix Backend && npm install --prefix Frontend && npm install",

    "frontend": "npm start --prefix Frontend",
    "backend": "npm start --prefix Backend",

    "app": "concurrently \"npm run frontend\" \"npm run backend\"",

    "docker:build": "docker build -t landing-solutions-dev .",
    "clean": "docker rm -f landing-frontend"
  },

  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}